@{
    ViewData["Title"] = "Письмо отправлено";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header bg-success text-white">
                    <h3 class="mb-0">Проверьте вашу почту</h3>
                </div>
                <div class="card-body text-center">
                    @if (TempData["SuccessMessage"] != null)
                    {
                        <div class="alert alert-info">
                            @TempData["SuccessMessage"]
                        </div>
                    }

                    <div class="my-4">
                        <i class="bi bi-envelope-check" style="font-size: 4rem; color: #28a745;"></i>
                    </div>

                    <p class="lead">Мы отправили инструкции по восстановлению пароля на вашу почту.</p>
                    <p class="text-muted">Если письмо не пришло в течение нескольких минут, проверьте папку "Спам".</p>

                    <div class="mt-4">
                        <a asp-action="Login" class="btn btn-primary">Перейти к входу</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
