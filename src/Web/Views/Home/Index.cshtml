@{
    ViewData["Title"] = "Главная";
}

<div class="container py-5">
    <div class="row align-items-center">
        <div class="col-lg-6">
            <h1 class="display-4 fw-bold mb-4">
                <i class="bi bi-robot text-primary"></i> BotConstructor
            </h1>
            <p class="lead mb-4">
                Платформа для создания и управления ботами для мессенджеров
            </p>
            <p class="mb-4">
                Создавайте чат-ботов без программирования. Используйте готовые шаблоны
                или создавайте свои уникальные сценарии взаимодействия с пользователями.
            </p>

            @if (User.Identity?.IsAuthenticated == true)
            {
                <div class="d-flex gap-3">
                    <a asp-controller="Profile" asp-action="Index" class="btn btn-primary btn-lg">
                        <i class="bi bi-person"></i> Мой профиль
                    </a>
                    @if (User.IsInRole("SuperAdmin") || User.IsInRole("Admin"))
                    {
                        <a asp-controller="Admin" asp-action="Users" class="btn btn-outline-primary btn-lg">
                            <i class="bi bi-shield-lock"></i> Админка
                        </a>
                    }
                </div>
            }
            else
            {
                <div class="d-flex gap-3">
                    <a asp-controller="Auth" asp-action="Register" class="btn btn-primary btn-lg">
                        <i class="bi bi-person-plus"></i> Начать бесплатно
                    </a>
                    <a asp-controller="Auth" asp-action="Login" class="btn btn-outline-primary btn-lg">
                        <i class="bi bi-box-arrow-in-right"></i> Войти
                    </a>
                </div>
            }
        </div>

        <div class="col-lg-6 mt-5 mt-lg-0">
            <div class="card shadow-lg border-0">
                <div class="card-body p-4">
                    <h3 class="card-title mb-4">
                        <i class="bi bi-star-fill text-warning"></i> Возможности платформы
                    </h3>
                    <ul class="list-unstyled">
                        <li class="mb-3">
                            <i class="bi bi-check-circle-fill text-success me-2"></i>
                            <strong>Визуальный конструктор</strong> - создавайте ботов без кода
                        </li>
                        <li class="mb-3">
                            <i class="bi bi-check-circle-fill text-success me-2"></i>
                            <strong>Готовые шаблоны</strong> - быстрый старт для любых задач
                        </li>
                        <li class="mb-3">
                            <i class="bi bi-check-circle-fill text-success me-2"></i>
                            <strong>Мультиплатформенность</strong> - Telegram, WhatsApp, VK и др.
                        </li>
                        <li class="mb-3">
                            <i class="bi bi-check-circle-fill text-success me-2"></i>
                            <strong>Аналитика</strong> - отслеживайте эффективность ботов
                        </li>
                        <li class="mb-3">
                            <i class="bi bi-check-circle-fill text-success me-2"></i>
                            <strong>Безопасность</strong> - защита данных и конфиденциальность
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    @if (User.Identity?.IsAuthenticated != true)
    {
        <div class="row mt-5">
            <div class="col-12 text-center">
                <h2 class="mb-4">Начните создавать своего бота прямо сейчас</h2>
                <p class="lead text-muted mb-4">
                    Присоединяйтесь к тысячам пользователей, которые уже автоматизировали общение с клиентами
                </p>
                <a asp-controller="Auth" asp-action="Register" class="btn btn-primary btn-lg px-5">
                    <i class="bi bi-rocket-takeoff"></i> Зарегистрироваться бесплатно
                </a>
            </div>
        </div>
    }
</div>
